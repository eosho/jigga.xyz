# KSOPS generator for SOPS-encrypted Proxmox secrets
apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: proxmox-secrets
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - config/proxmox-ssh-secret.yaml
  - config/postgres-credentials-secret.yaml
